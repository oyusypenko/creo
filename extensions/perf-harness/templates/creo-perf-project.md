# creo-perf extension — __PROJECT_ID__

Project knowledge for the creo-perf skill and the creo-perf-audit agent.
Loaded before any measurement or fix. Generated by `/creo perf init`; keep it
current — every capture and every finding should be readable from here.

## Stack

| Layer | What | Where |
|---|---|---|
| Database | PostgreSQL NN (+ extensions) | container `my-postgres` :5432, db `db`, user `user` |
| Backend | FastAPI / Node / ... | `backend/` — hot path `backend/app/route/items.py` |
| Edge | nginx / none | `frontend/nginx.conf` — gzip_types, proxy_pass |
| Frontend | Vite + React / Next / ... | `frontend/` — data layer `src/app.tsx`, hot page `src/pages/items/` |

## Targets (mirror of perf.config.sh)

| Target | URL | Notes |
|---|---|---|
| API direct | http://localhost:8001 | stable numbers; local dev server only for iteration, never mixed into a pair |
| API via proxy | http://localhost:80/api | compression / ETag / proxy overhead exist ONLY here |
| Frontend | http://localhost:80 | prod build; dev server inflates render times 2-5x |
| DB | `docker exec -i my-postgres psql -U user -d db` | |

## Scenarios

| ID | Scenario | Primary endpoint -> handler | SQL runners | FE key |
|---|---|---|---|---|
| s1 | user opens the main list page (page 1, default sort) | `GET /items` -> `list_items()` `items.py:NN` | s1_default | next-page |

Ordering rules for `audit-all`: (list cache-churning scenarios that must run last and why).

## Hot path map

- Backend handler(s): `backend/app/route/items.py:NN-NN`
- Query builder / filters: ...
- Schema and indexes: `db/schema.sql`
- Frontend page: `frontend/src/pages/items/index.tsx`, data layer `frontend/src/app.tsx` (QueryClient)
- Proxy / compression: `frontend/nginx.conf`

## Hazards

- (files never to open, commands never to run, ports that collide, env vars unavailable)
- Never `docker compose down -v`; never `ANALYZE` mid-audit; never run two captures concurrently

## Environment log

| Date | Change | Validated by |
|---|---|---|
| YYYY-MM-DD | observability enabled (pg_stat_statements + auto_explain) | `obs-check` capture: DB medians within noise of `before` |

## Findings ledger

One row per finding, in commit order. Numbers come from `results/dashboard.md`.

| # | Scenario | Layer | Bottleneck | Fix | Before -> After | Commit |
|---|---|---|---|---|---|---|
